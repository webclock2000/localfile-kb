# FileKB Streamlit Configuration
# No [theme] section — let Streamlit follow system light/dark mode.
# Fonts are set via CSS in app.py.

[server]
headless = true
enableStaticServing = true

[browser]
gatherUsageStats = false
